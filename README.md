# Discord Ticket Bot

## Description

An AI-powered Discord ticket bot that can auto-categorize issues, suggest responses, and track resolution times.

## Setup

1.  Install dependencies: `npm install`
2.  Configure environment variables (see `.env.example`)
3.  Run the bot: `npm start`

## Configuration

See `.env.example` for configurable environment variables.

## Usage

(To be completed)
